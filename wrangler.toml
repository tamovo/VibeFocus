name = "vibefocus"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# Environment variables are set in Cloudflare Pages dashboard → Settings → Environment variables:
#   GOOGLE_CLIENT_ID      — used by Pages Functions (server-side token verification)
#   VITE_GOOGLE_CLIENT_ID — used by Vite at build time (Google OAuth button)

[[d1_databases]]
binding = "DB"
database_name = "vibefocus-db"
database_id = "e2f79f9b-78c9-42f9-a357-eb92969dfd00"
